services:
  notify:
    build: .
    ports:
      - "8080:8080"
    environment:
      CONFIG_PATHS: /app/config
      DB_PATH: /app/data/peek.db
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
