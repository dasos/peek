display_name: DIUN
fields:
  badge: "Update"
  title: "{{ image }} has been updated"
  link: "{{ hub_link }}"
  coalesce: "{{ image.split('/')[2].split(':')[0] }}"
  description: |
    {{ metadata.ctn_name }} has been updated. New image: [**{{ image }}**]({{ hub_link }})
